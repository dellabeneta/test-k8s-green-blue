apiVersion: apps/v1
kind: Deployment
metadata:
  name: webcolor
spec:
  replicas: 18
  selector:
    matchLabels:
      app: webcolor
  template:
    metadata:
      labels:
        app: webcolor
    spec:
      containers:
      - name: webcolor
        image: dellabeneta/web-page:green
        ports:
        - containerPort: 80
